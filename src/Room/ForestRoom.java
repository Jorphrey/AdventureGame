package Room;

public class ForestRoom extends Room{
    public ForestRoom(boolean isLocked, boolean hasEnemy, boolean hasLoot, boolean hasPuzzle) {
        super(isLocked, hasEnemy, hasLoot, hasPuzzle);
    }
}
